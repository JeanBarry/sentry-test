services:
  web:
    container_name: web
    build: 
      context: ../
      dockerfile: ./docker/Dockerfile
      args:
        NODE_ENV: $NODE_ENV
    ports:
      - 3000:3000
    volumes:
      - ../src:/usr/src/app/src
    command: $RUN_COMMAND